extends base.pug
block title
    | 新楓之谷 - 淫小築 - 入會註冊

block content
    div.uk-section.uk-section-muted
        div.uk-container.uk-container-xsmall
            h1.uk-heading-line.uk-text-center
                | 輸入邀請碼

            div.uk-alert
                div.uk-margin
                    | 請聯繫淫小築公會幹部，並通過面試索取邀請碼。

                div.uk-margin
                    span.uk-label.uk-margin-right
                        | 請留意
                    | 每組代碼是遊戲角色綁定的，無法重複使用或轉帳給他人。

            form(action='/register', method='post')
                div.uk-margin
                    label.uk-form-label(for='inviteCode')
                        | 邀請代碼
                    div.uk-form-controls
                        input#inviteCode.uk-input(name='inviteCode', type='text')

                div.uk-margin
                    label.uk-form-label(for='displayName')
                        | LINE
                    div.uk-form-controls
                        div.uk-inline
                            span.uk-form-icon(uk-icon='icon: user')
                            input#displayName.uk-input(name='displayName', type='text', value= displayName, readonly)

                input#lineID(name='lineID', type='hidden', value= lineID)

                input#pictureURL(name='pictureURL', type='hidden', value= pictureURL)

                input.uk-button.uk-button-primary.uk-align-right(type='submit', value='送出')
