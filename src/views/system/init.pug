extends ../base.pug
block title
    | 系統首次建置

block content
    div.uk-section.uk-section-muted
        div.uk-container.uk-container-xsmall
            h1.uk-heading-line.uk-text-center
                | 系統首次建置

            div.uk-alert
                div.uk-margin
                    | 請先透過&nbsp;
                    a.uk-link(href= authURL target='_blank')
                        | Google OAuth
                    | &nbsp;進行登入授權，並將代碼 code 回填於下方。

                div.uk-margin
                    span.uk-label.uk-margin-right
                        | 請留意
                    | 進行登入的帳號，必須擁有編輯該 spreadsheet 的權限。

            form(action='/system/init', method='post')
                div.uk-margin
                    label.uk-form-label(for='code')
                        | 代碼
                    div.uk-form-controls
                        input#code.uk-input(name='code', type='text')

                input.uk-button.uk-button-primary.uk-align-right(type='submit', value='送出')
